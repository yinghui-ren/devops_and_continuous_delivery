# AAP DevOps POC

Simple Node.js Express application for testing the CI/CD pipeline.

## Endpoints

```text
GET /        -> Hello from AAP DevOps POC
GET /health  -> { "status": "OK" }
```

## Run locally

```bash
npm install
npm test
npm start
```

The application runs on port `3000` by default.
